# מחקר עדכון — 18 באוגוסט 2026

**מטרה:** ריצת עדכון שבועית לפי `docs/weekly-update-pipeline.md`, מכסה את הפרק שבין העדכון הקודם (`docs/2026-08-update-research.md`, מחקר מ-6.8.2026, הוזרק בקומיט `317356b` ב-8.8.2026) לבין היום.
**סטטוס:** הוזרק ל-`anchor-votes.json` ו-`parties.json`, `npm run update-scores` הורץ, `build`/`lint`/JSON עברו נקי. **לא נדחף (push)** — commit מקומי בלבד.

---

## 0. תזכורת מתודולוגית

כרגיל: עובדות מאומתות (הצבעות, תאריכים, סקרים מיוחסים) → `actual`/`seats`. תעמולת קמפיין → `declared` בלבד, ורק כשניתן לנסח כעמדה בדוקה. שום דבר לא הוזרק בלי 2+ מקורות (למעט נתוני API רשמיים, שהם מקור יחיד אך מוסמך).

---

## 1. ממצאים

### 1.1 מפלגת ארדן–אדלשטיין קיבלה שם רשמי: "האחדות"
נחשף ב-11.8.2026. פותר את החסם שדווח במחקר הקודם ("שם טרם אותר").

מקורות: [N12](https://www.mako.co.il/news-israel-elections/2026/Article-ce0f6948cb1ff91026.htm), [וואלה](https://news.walla.co.il/item/3860090)

**לא נוסף כמפלגה חדשה** — ר' סעיף 3 (מתחת לאחוז החסימה בסקרים).

### 1.2 קריסת "המחנה הממלכתי" (בני גנץ) מתחת לאחוז החסימה ⚠️ ממצא מרכזי
סקרי אוגוסט 2026 (חדשות 12 ואחרים) מראים את כחול לבן/המחנה הממלכתי בטווח **1.4%–1.6%** — משמעותית מתחת ל-3.25% הנדרשים לכניסה לכנסת. גם צבירת סקרי `skarim.org` (18.8.2026, ממוצע 5 סקרים אחרונים) מציגה מפורשות את "כחול לבן" ברשימת המפלגות שמתחת לאחוז החסימה (לצד האחדות, בל"ד, המפלגה הכלכלית, זהות) — **ולא כלל אותם בטבלת המנדטים הראשית**.

זו ירידה חדה ממצב קודם (12 מנדטים בנתוני האתר, לפי מחקר קודם/סקרי יולי).

מקורות: [כאן — לובי הסקרים](https://www.kan.org.il/lobby/skarim/) (סקרי חדשות 12, אוגוסט 2026, 1.4%–1.6% לכחול לבן), [skarim.org/average-poll](https://www.skarim.org/average-poll) (נשלף 18.8.2026)

**פעולה שבוצעה:** עודכן `seats` ל-`mahaneh_mamlahti` מ-12 ל-**0**, בהתאם למיצוב המפורש "מתחת לאחוז החסימה" בשני מקורות עצמאיים. **לא בוצע שינוי מבני** (הישות `mahaneh_mamlahti` נשארה בנתונים — לא הוסרה) — רק עדכון תחזית מנדטים, כנדרש בפייפליין. אם ההתאוששות תחזור בסקרים עתידיים יש לעדכן בחזרה.

### 1.3 עדכון מנדטים כללי (ר' טבלה בסעיף 4) — 2+ מקורות לכל שינוי
צובר `skarim.org` (ממוצע 5 סקרים, 18.8.2026), מאומת מול כותרות סקרים בודדים מהשבוע: ערוץ 11 (16.8), ערוץ 12 (17.8), ערוץ 13 (12.8).

מקורות: [סקר כאן 11 — 16.8](https://www.haaretz.co.il/news/elections/2026-08-16/ty-article/000001a0-0bac-d75f-a3be-1bfe32f80000), [סקר חדשות 12 — 17.8](https://www.haaretz.co.il/news/elections/2026-08-17/ty-article/.premium/000001a0-10fc-d0aa-abfb-befd2beb0000), [Times of Israel — סקר 12.8](https://www.timesofisrael.com/fresh-election-poll-shows-both-blocs-shy-of-majority-new-right-wing-parties-fall-short/), [skarim.org/average-poll](https://www.skarim.org/average-poll)

### 1.4 הצבעת עוגן חדשה נמשכה מה-API: חוק פיצול/החלשת סמכויות היועמ"ש
זהה מהמחקר הקודם כ"מועמד חזק מאוד" אך לא הוזרקה אז בגלל בעיית מיפוי `MkId`↔`PersonID` שדווחה כפתוחה. **הבעיה נראית פתורה כעת** — `npm run fetch-vote -- "היועץ המשפטי" --after 2026-07-01` מצא ומיפה 113/116 קולות (רק 3 ח"כים בודדים לא ממופים, כנראה אותו artifact של פורמט שמות פקטורים שתואר בסעיף 1.5), ותוצאת ההצבעה (65 בעד / 51 נגד) תואמת בדיוק לדיווחי העיתונות.

- Vote ID: 46462, תאריך 15.7.2026, קריאה שלישית.
- נוסף כ-`attorney_general_powers_split_2026` ב-`anchor-votes.json`, בצירים `rule_of_law: min` + `authority_vs_checks: max` (עקבי עם `reasonableness_law_2023`/`judicial_committee_reform_2023`), משקל 2.0 — כחלק מאותה חבילת חוקים קואליציונית של יולי 2026 (יחד עם `basic_law_torah_study_2026` ו-`security_service_arrest_freeze_2026` שכבר בנתונים).
- הרצת `npm run update-scores` בעקבות התוספת עדכנה ציר אחד: `authority_vs_checks` בפועל של `beyahad` (2.5 → 2.1).

מקור: API הכנסת (OdataV4/ParliamentInfo, VoteID 46462), נמשך ואומת ישירות.

### 1.5 `sync-parties.js` — דיווח שינויים כוזב (false positive), לא שינוי אמיתי
`npm run sync-parties` דיווח "שינויים" (הליכוד, הציונות הדתית, המחנה הממלכתי, רע"ם, הימין הממלכתי כ"סיעות חדשות"; הליכוד, המחנה הממלכתי, רע"מ, בית ציוני כ"מפלגות שהוסרו"). **נבדק ידנית — אלו artifacts של פורמט, לא שינויים פוליטיים אמיתיים:**
- `"הליכוד "` (עם רווח סופי) לעומת `"הליכוד"` הממופה ב-`KNOWN_MAPPING`.
- `"רע\"ם"` (מ' סופית) לעומת `"רע\"מ"` הממופה (מ' לא-סופית) — אותה תופעה שנראתה גם ב-3 הקולות הלא-ממופים בסעיף 1.4.
- `"הציונות הדתית"` / `"המחנה הממלכתי"` — חזרו בלי הסיומת המלאה ("בראשות...", "כחול לבן -") שב-`KNOWN_MAPPING`.
- `beit_tzioni` דווח כ"הוסר" כי הוא לא ברשימת `preElectionParties` שהסקריפט מדלג עליה (יחד עם `beyahad`/`yisrael_yashar`/`hademokratim`) — פער ידוע בסקריפט, לא שינוי אמיתי.

**מסקנה:** ככל הנראה ה-API עצמו שינה מעט את פורמט השמות המוחזרים בטבלת `KNS_Faction` אחרי פיזור הכנסת (7.17.2026) — לא זוהה שום פיצול/מיזוג/שינוי סיעה אמיתי בשום מקור עיתונאי מהשבוע. **לא בוצע שינוי בנתונים בעקבות זה.** מומלץ לתחזוקה עתידית (לא בוצע כעת — מחוץ לסקופ של עדכון תוכן שבועי): לנרמל רווחים/מרכאות ב-`KNOWN_MAPPING`, ולהוסיף `beit_tzioni` לרשימת `preElectionParties`.

### 1.6 הרשימה הערבית המשותפת — עדיין לא נסגרה, נסיגה מהמו"מ
ב-9.8.2026 ועדת הפיוס הלאומית קבעה סדר רשימה (חד"ש 5 מהעשירייה הראשונה, בל"ד 3, תע"ל 2) — **בל"ד אישרה, תע"ל (טיבי) דחתה** וקפאה את המגעים, בטענה שהרוטציה המוצעת לא מבטיחה מימוש הבטחות. טיבי נתן אולטימטום ("עד יום חמישי"). טור דעה בהארץ (11.8) מציע שטיבי "פספס את ההזדמנות". **מצב עדיין נזיל** — תואם בדיוק את ההנחיה במחקר הקודם לא להזריק שינוי מבני ולעקוב שוב סמוך ל-7.9.2026.

מקורות: [מעריב](https://www.maariv.co.il/news/politics/Article-679321), [כאן — לייב](https://www.kan.org.il/content/kan-news/politic/live-1087009), [הארץ — דעה](https://www.haaretz.co.il/opinions/2026-08-11/ty-article-opinion/.premium/0000019f-f190-d569-a5ff-f1f480890000)

**לא הוזרק שינוי מבני.**

### 1.7 מיזוג הליכוד–תקווה חדשה (סער) — עדיין באוויר
לא נמצא עדכון/הכרעה חדשים מעבר למה שכבר תועד במחקר הקודם (נתניהו "שוקל" לבטל את המיזוג). **לא הוזרק שינוי.**

### 1.8 בג"ץ נגד חוק יסוד: לימוד תורה — טרם הכרעה
העתירה של התנועה לאיכות השלטון עדיין תלויה ועומדת; לא נמצאה החלטת בג"ץ מהשבוע האחרון. אין פעולה נדרשת — ההצבעה עצמה כבר בנתונים.

### 1.9 אישור: הכנסת פוזרה רשמית ב-17.7.2026
מקור: [ישראל היום](https://www.israelhayom.co.il/news/politics/article/20659246) ואחרים. מסביר מדוע לא נמצאו הצבעות מליאה חדשות מאז אמצע יולי — אין פעילות מליאה עד להשבעת הכנסת ה-26 (10.11.2026 לפי תוכנית).

---

## 2. מה הוזרק בפועל

| קובץ | שינוי |
|---|---|
| `src/data/anchor-votes.json` | נוספה `attorney_general_powers_split_2026` (VoteID 46462, 15.7.2026, 65–51) |
| `src/data/parties.json` | `seats`: likud 25→24, yisrael_yashar 23→24, beyahad 11→12, hadash_taal 3→4, **mahaneh_mamlahti 12→0** |
| — | `npm run update-scores` הורץ; עדכן `beyahad.actual.authority_vs_checks` 2.5→2.1 |
| `src/components/About.tsx` / `Welcome.tsx` / `Results.tsx` | **לא נדרש שינוי** — התאריכים הקיימים ("אוגוסט 2026" / "עד יולי 2026" / "data 2026-08") עדיין מדויקים, כי העדכון הזה עדיין בתוך אוגוסט ולא נוספה הצבעה אחרי יולי |

## 3. מה לא הוזרק, ולמה

| פריט | סיבה |
|---|---|
| מפלגת "האחדות" (ארדן-אדלשטיין) | שם ידוע כעת, אך מתחת לאחוז החסימה בכל הסקרים העדכניים (מפורש ב-`skarim.org` וב"כאן") — עקבי עם הקריטריון שנקבע במחקר הקודם (סעיף 1.10) לא לכלול מפלגות שוליות מתחת לסף. מוכנה להזרקה אם תתחיל לעבור סף באופן עקבי. |
| מבנה הרשימה הערבית המשותפת | עדיין נזיל — תע"ל דחתה את הצעת ועדת הפיוס השבוע. לעקוב שוב לקראת 7.9.2026. |
| ביטול מיזוג הליכוד–תקווה חדשה | עדיין רק "בשיקול", לא הוכרע. |
| נרמול `KNOWN_MAPPING`/`preElectionParties` ב-`sync-parties.js` | false positives מתועדים בסעיף 1.5; תיקון קוד — מחוץ לסקופ עדכון תוכן שבועי, לא באג בנתונים. |

---

## 4. טבלת מנדטים — לפני/אחרי

| מפלגה (`id`) | קודם | עכשיו | מקור |
|---|---|---|---|
| `likud` | 25 | 24 | skarim.org ממוצע 18.8 |
| `yisrael_yashar` | 23 | 24 | skarim.org ממוצע 18.8 |
| `beyahad` | 11 | 12 | skarim.org ממוצע 18.8 |
| `hadash_taal` | 3 | 4 | skarim.org ממוצע 18.8 |
| `mahaneh_mamlahti` | 12 | **0** | סקרי חדשות 12 (1.4%–1.6%) + skarim.org (מפורש: מתחת לסף) |
| `hademokratim`, `israel_beiteinu`, `shas`, `yahadut_hatorah`, `otzma_yehudit`, `hazionyut_hadatit`, `raam`, `beit_tzioni` | — | ללא שינוי | יציבים בממוצע הסקרים |

---

## 5. בדיקת תקינות

- `node -e "JSON.parse(...)"` על שלושת קבצי הנתונים → ✅ תקין
- `npm run build` (tsc -b && vite build) → ✅ עבר, 0 שגיאות
- `npm run lint` (oxlint) → ✅ 0 ממצאים
- `npm run update-scores` → ✅ רץ נקי, עדכן ציר אחד כצפוי

### ⚠️ פער בבדיקת E2E בדפדפן (שלב 5 בפייפליין)
ריצה זו בוצעה בסשן משימה מתוזמנת (unattended) — כלי הדפדפן (`preview_start`) חסם הפעלת שרת פיתוח מסיבות בטיחות ("Dev servers can't be started from unattended sessions"). **לא בוצעו 2 מעברים מלאים על השאלון בדפדפן כנדרש בשלב 5.** כתחליף חלקי: אומת ש-`seats` הוא שדה תצוגה בלבד ואינו משמש בשום חילוק/חישוב ב-`matching.ts`/`Results.tsx`/`App.tsx` — כך שהערך 0 עבור `mahaneh_mamlahti` לא אמור לגרום ל-NaN/קריסה. **מומלץ שהמשתמש יריץ בדיקה ידנית קצרה בדפדפן** (במיוחד לוודא ש`mahaneh_mamlahti` עם 0 מנדטים מוצג כראוי ולא שובר את תצוגת התוצאות) לפני/אחרי push.

---

## 6. סיכום להחלטה אנושית (לא הוכרע אוטומטית)

1. **"האחדות"** — לעקוב; להזריק כמפלגה רק אם תעבור סף בעקביות.
2. **הרשימה הערבית המשותפת** — לעקוב לקראת 7.9.2026 (נעילת רשימות).
3. **מיזוג הליכוד/תקווה חדשה** — לעקוב.
4. **קריסת המחנה הממלכתי ל-0 מנדטים** — עודכן אוטומטית לפי 2 מקורות עצמאיים ומפורשים; לא ישות שנויה במחלוקת אך זו ירידה חדה שכדאי שמשתמש אנושי יאשר/יבחן לפני push (למשל האם רוצים סף מינימלי >0 לצורכי תצוגה, גם אם הסקרים מראים 0).
