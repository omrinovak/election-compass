# משימה: ביקורת איכות על הוספת "עמך ישראל"

**מתי להשתמש בזה:** רק אחרי שהשיחה שהריצה את `docs/handoff-amcha-yisrael.md` סיימה בפועל — כלומר אחרי שמפלגת "עמך ישראל" כבר נוספה ל-`parties.json`, נבנה עבורה פרופיל מלא (`declared`/`actual`/`confidence`/`reliability`/`gaps`), ונכתב לפחות קובץ מחקר-מועמד אחד (`docs/candidate-research/amcha_yisrael-*.md`), ו-build/lint/tsc עברו נקי. אם זה עדיין לא קרה — אל תריץ את זה עדיין.

הדבק את הפרומפט שלמטה כהודעה לשיחת Claude Code **חדשה** בפרויקט `C:\Users\omrin\OneDrive\מצפן בחירות`.

---

## הפרומפט

אתה עורך ביקורת איכות עצמאית על נתוני מפלגה חדשה שנוספה לאחרונה לאפליקציית התאמת-מצביע "מצפן הבחירות" ב-`C:\Users\omrin\OneDrive\מצפן בחירות`. אין לך זיכרון של איך הנתונים הופקו — התייחס אליהם באותה ספקנות כמו לקוד של מישהו זר. המנדט שלך: למצוא בעיות אמיתיות **ולתקן** אותן, לא רק לרשום אותן. העקרונות המנחים הם (1) המתודולוגיה של הפרויקט עצמו, ו-(2) עובדות מאומתות — לעולם אל תסמוך על סיכום בלי לבדוק את המקור הראשוני.

## רקע

`src/data/parties.json` מכיל אובייקטי מפלגה, וכל אחד מכיל מערך `candidates`. לאחרונה נוספה מפלגה חדשה, `amcha_yisrael` ("עמך ישראל", בראשות עופר וינטר) — המפלגה ה-13 בקובץ, והראשונה שנוספה כמפלגה חדשה-לגמרי (לא עדכון מנדטים למפלגה קיימת) מאז תחילת הפרויקט. ההוספה בוצעה בשיחה נפרדת לפי הוראות ב-`docs/handoff-amcha-yisrael.md` (קרא אותו קודם — הוא מסביר את הרקע המלא: המפלגה נמצאה כחוצה את אחוז החסימה במחקר-סקרים מ-22.9.2026, `docs/2026-09-22-update-research.md` סעיף 1.1).

המתודולוגיה המלאה למחקר-מועמד מתועדת ב-`docs/candidate-research/beyahad-lapid.md` — קרא אותו במלואו. כללי הליבה: אפס בדיה (כל ציטוט חייב להיות מאומת מול מקור חי לפני שהוא משמש; מה שלא ניתן לאמת מושמט או מסומן בביטחון מופחת, אף פעם לא מנוחש); אימות סדר-רשימה מול 2-3 מקורות לפחות; ציוני צירים על סולם 1-7 עם ביטחון 0-1, מכוילים מול ההגדרה בפועל של הציר באפליקציה. `src/utils/matching.ts` (`MIN_CANDIDATE_AXES = 4`) — למועמד עם פחות מ-4 צירים מנוקדים זה תקין וצפוי, לא באג.

מתודולוגיית מפלגה-חדשה-ללא-רקורד מתועדת ב-`docs/weekly-update-pipeline.md`: `actual` ≈ `declared`, `confidence` נמוך (0.25–0.55), `reliability.promise_keeping`/`execution` = `null`/0.15.

## שלב 1 — שלמות סכימה

1. הרץ `npx tsc --noEmit -p .` ו-`npm run build` ווודא בסיס ירוק.
2. בדוק את אובייקט המפלגה `amcha_yisrael`: כל 21 שמות הצירים ב-`declared`/`actual`/`confidence` תואמים בדיוק לרשימה הקנונית (חפש `AXIS_LABELS` ב-`matching.ts` או `questions.json`); ערכי `declared`/`actual` בטווח 1-7; ערכי `confidence` בטווח 0-1 ונמוכים (0.25-0.55) כמצופה למפלגה חדשה; `reliability.promise_keeping.score`/`execution.score` הם `null`/0.15 בדיוק לפי הקונבנציה; `gaps` הוא מערך (ריק זה תקין למפלגה חדשה).
3. בדוק את מערך `candidates`: `position` תקין, אין כפילויות, `actual: {}` תמיד, מפתחות זהים בין `declared`/`confidence`/`sources` לכל מועמד.
4. ודא ש-`seats` הוא 4 (או המספר שתועד בפועל בקובץ המחקר של השיחה הקודמת אם השתנה).

## שלב 2 — דיוק תעתוק

לכל מועמד שנוסף (עופר וינטר לפחות, ואם נוספו מקומות 2-4 — גם הם): פתח את קובץ ה-md התואם ב-`docs/candidate-research/` והשווה שורה-שורה מול הרשומה ב-JSON. חפש: ציר שמנוקד ב-md אבל חסר ב-JSON (או להפך); מספר שלא תואם בין הטבלה ל-JSON; ציטוטים/דוברים/תאריכים/URL-ים ב-`externalViews` שלא תואמים מילה-במילה למה שב-md; שם שלא תואם.

## שלב 3 — בדיקת עובדות בפועל

בחר לפחות 4-5 ציטוטים (עדיפות ל-`externalViews` ולכל ציטוט שקובץ המחקר עצמו סימן בביטחון מופחת/אימות חלקי) ותשלוף בפועל את ה-URL המצוטט. אם מקור לא נגיש — נסה לאמת עצמאית דרך חיפוש. ציטוט שלא מאומת (מזויף, מיוחס לאדם הלא נכון, או שונה מהותית ממה שנטען) — תקן או הסר את הציון/המקור, הורד ביטחון בהתאם, ותעד בבירור.

## שלב 4 — בדיקת עקביות מול שאר הפרויקט

1. ודא ש-`amcha_yisrael` לא מתנגש בצבע (`color`) עם אף מפלגה קיימת אחרת ב-`parties.json`.
2. ודא ש-`id: "amcha_yisrael"` לא יוצר קונפליקט/כפילות במקום אחר בקוד (חפש `amcha_yisrael` בכל `src/`).
3. אם המחקר החדש משתמש בציר `ideology_vs_pragmatism` — ודא שהכיוון (גבוה=פרגמטיות) תואם לקונבנציה שכבר תוקנה בכל שאר המפלגות (ר' `questions.json` — התוויות כבר תוקנו בביקורת קודמת; אל תהפוך אותן שוב).
4. הרץ `npm run update-scores` מחדש (ליתר ביטחון, למקרה שהשיחה הקודמת לא הריצה אותו אחרי ההוספה) ובדוק שאין שינויים בלתי-צפויים במפלגות אחרות.

## כללי עבודה

- אל תבדה/תנחש תיקון — אם אי אפשר לאמת, הורד ביטחון או הסר, בדיוק לפי כלל אפס-הבדיה של הפרויקט.
- שמור על סנכרון בין קובצי ה-md ל-`parties.json` — אם תיקנת אחד, תקן גם את השני.
- אל תעשה commit או push — זו ביקורת על working tree מקומי בלבד.
- אחרי כל תיקון: הרץ שוב `npx tsc --noEmit -p .` ו-`npm run build` ואשר שעדיין ירוקים.

## דוח סופי

כתוב בעברית: מה נבדק (מספרים), מה נמצא (רשימה ברורה: תקין / תוקן / מסומן לבדיקה אנושית, עם שם המועמד/קובץ לכל פריט), וסטטוס ה-build/typecheck הסופי.
