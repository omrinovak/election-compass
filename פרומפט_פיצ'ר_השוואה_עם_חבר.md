# פרומפט לסוכן קוד — פיצ'ר "השוואה עם חבר" + עדכון ניסוח זמן השאלון

**להעתיק את כל הקובץ הזה כפי שהוא ולתת לסוכן הקוד (Claude Code או כל כלי דומה) שעובד על הריפו של matzpen-bchirot.**

הקשר: אפליקציית React (Vite) שרצה כולה בדפדפן — **אין שרת, אין איסוף מידע אישי, אין DB**. כל תיקון חייב לשמור על זה: שום דבר לא נשלח לשום שרת, כל ה"שיתוף" עובר דרך ה-URL עצמו.

---

## משימה 0 — עדכון ניסוח (קטן, לעשות ראשון)

בקובץ `src/components/Welcome.tsx`, השורה עם:
```tsx
<div className="time-badge">
  <ClockIcon />
  5–7 דקות
</div>
```
לשנות ל-`10–15 דקות` (לשמור על ה-en dash "–" כמו במקור, לא מקף רגיל).

---

## משימה 1 — פיצ'ר "השווה עם חבר", עם אישור הדדי של שני הצדדים

### דרישת-על (לא לוותר עליה)
התוצאה של אף אחד מהצדדים **לא נחשפת לצד השני עד ששניהם אישרו במפורש**:
1. **הצד ששולח** — חייב לעבור מסך אישור מפורש לפני שנוצר הקישור (לא "שיתוף אוטומטי").
2. **הצד שמקבל** — לא רואה שום השוואה עד שהוא **בעצמו סיים לענות על כל השאלון** (36 שאלות, לא קיצור דרך). זו ה"הסכמה" של הצד השני — השקעה מלאה בשאלון, לא קליק בודד.

### שלב א' — לחשוף פרופיל צירים (axis profile) שאפשר לקודד בקישור

בקובץ `src/utils/matching.ts`:
- כרגע `buildUserProfile` היא פונקציה פנימית (לא exported) שמחזירה `Record<string, {value, weight, count}>`.
- להוסיף פונקציה מיוצאת חדשה:
```ts
export function getAxisProfile(answers: Answer[], priorities: string[]): Record<string, number> {
  const profile = buildUserProfile(answers, priorities);
  const result: Record<string, number> = {};
  for (const axis of Object.keys(profile)) {
    if (profile[axis].count > 0) result[axis] = Math.round(profile[axis].value * 10) / 10;
  }
  return result;
}
```
- להוסיף גם פונקציית השוואה בין שני פרופילים (להשתמש ב-`axisSimilarity` הקיימת, שכרגע פרטית — אפשר להשאיר אותה פרטית ולהשתמש בה מבפנים):
```ts
export function compareProfiles(
  a: Record<string, number>,
  b: Record<string, number>
): { compatibility: number; perAxis: Record<string, number>; sharedAxes: string[] } {
  const shared = Object.keys(a).filter((axis) => axis in b);
  const perAxis: Record<string, number> = {};
  let sum = 0;
  for (const axis of shared) {
    const sim = axisSimilarity(a[axis], b[axis]);
    perAxis[axis] = sim;
    sum += sim;
  }
  return {
    compatibility: shared.length > 0 ? sum / shared.length : 0,
    perAxis,
    sharedAxes: shared,
  };
}
```

### שלב ב' — קידוד/פענוח לינק (קובץ חדש)

יצירת `src/utils/compareLink.ts`:
```ts
interface CompareLinkPayload {
  v: 1;
  p: string;           // top party id של השולח
  s: number;            // overallScore מעוגל (0-100)
  ax: Record<string, number>; // axis profile
}

export function encodeCompareProfile(ax: Record<string, number>, partyId: string, score: number): string {
  const payload: CompareLinkPayload = { v: 1, p: partyId, s: Math.round(score * 100), ax };
  return btoa(encodeURIComponent(JSON.stringify(payload)));
}

export function decodeCompareProfile(raw: string): CompareLinkPayload | null {
  try {
    const parsed = JSON.parse(decodeURIComponent(atob(raw)));
    if (parsed && parsed.v === 1 && parsed.ax && typeof parsed.p === 'string') {
      return parsed as CompareLinkPayload;
    }
    return null;
  } catch {
    return null;
  }
}
```
חשוב: פענוח שנכשל (לינק פגום/ישן) — **לא לזרוק שגיאה למשתמש**, סתם להתעלם ולהתנהג כמו כניסה רגילה לאתר.

### שלב ג' — App.tsx: לקלוט לינק נכנס, לשמור את הפרופיל של המשתמש הנוכחי

ב-`src/App.tsx`:
- ב-mount (או ב-state init), לקרוא `new URLSearchParams(window.location.search).get('cmp')` ולנסות `decodeCompareProfile`. לשמור ב-state: `const [friendProfile, setFriendProfile] = useState<CompareLinkPayload | null>(...)`.
- ב-`handlePrioritiesComplete`, לצד `calculateResults(answers, p)`, לחשב גם `getAxisProfile(answers, p)` ולשמור ב-state חדש (`const [axisProfile, setAxisProfile] = useState<Record<string, number>>({})`).
- להעביר `friendProfile` ל-`Welcome` (להצגת באנר) ול-`Results` (להצגת ההשוואה + לבניית קישור יוצא), ולהעביר גם `axisProfile` ל-`Results`.

### שלב ד' — Welcome.tsx: באנר כשמגיעים דרך לינק השוואה

כש-`friendProfile` לא null, להציג מעל/ליד ה-time badge משהו כמו:
> 🤝 חבר/ה שלח/ה לך בקשה להשוות תוצאות. כדי לראות את ההשוואה, קודם צריך לענות על השאלון (10–15 דקות) — ואז ההשוואה תופיע אוטומטית.

לא לשנות שום דבר אחר בזרימה — הכפתור "בואו נתחיל" ממשיך להוביל לאותו שאלון רגיל.

### שלב ה' — Results.tsx: כפתור יצירת קישור + מסך אישור, וכפתור/כרטיס תצוגת ההשוואה

**יצירת קישור יוצא (לכל משתמש, גם בלי `friendProfile`):**
- קומפוננטה חדשה `CompareWithFriend({ top, axisProfile })` ליד `<ShareCard top={top} />` הקיימת.
- קליק ראשון פותח מסך/מודל אישור קטן (state toggle פנימי, לא alert/confirm של הדפדפן — לא להשתמש ב-`window.confirm`):
  > משתפים קישור אישי עם חבר/ה. הם יראו את ההשוואה ביניכם רק **אחרי** שגם הם יענו על השאלון בעצמם. להמשיך?
  עם כפתורי "כן, שתפו" / "ביטול".
- רק אחרי אישור — לבנות את הקישור: `${QUESTIONNAIRE_URL}?cmp=${encodeCompareProfile(axisProfile, top.id, top.overallScore)}`, ולהשתמש באותו דפוס שיתוף שכבר קיים ב-`ShareCard` (native share / העתקת קישור / וואטסאפ), עם טקסט מותאם, למשל:
  `אני ${top.name} (${pct(top.overallScore)} התאמה). בוא נבדוק כמה אנחנו פוליטית תואמים — ${LINK}`
- לשלוח אירוע Umami חדש: `umami.track('compare_link_created', { top_party: top.id })`.

**תצוגת ההשוואה (רק כש-`friendProfile` קיים וגם `results`/`axisProfile` של המשתמש הנוכחי כבר מחושבים — כלומר המשתמש כבר סיים את השאלון שלו):**
- להשתמש ב-`compareProfiles(axisProfile, friendProfile.ax)` מ-`matching.ts`.
- להציג כרטיס `CompareResultCard` בראש עמוד התוצאות (מעל או ליד ה-`ShareCard`): אחוז התאמה כללי בין שני המשתמשים, ציר אחד עם ההסכמה הגבוהה ביותר, ציר אחד עם הפער הגדול ביותר — ממש כמו הרעיון "הנושא שהכי מסכימים / הנושא שהכי חלוקים" (למיין את `perAxis` מ-`compareProfiles`, לקחת max ו-min, ולהציג עם `getAxisLabel`).
- אם `sharedAxes.length === 0` — להציג הודעת נפילה רכה ("אין מספיק נתונים משותפים להשוואה") במקום להתרסק או להציג NaN.
- להוסיף כפתור "שלחו גם אתם השוואה הלאה" שמייצר קישור חדש (אותה לוגיקה כמו ב-`CompareWithFriend`) — כדי שההשוואה תמשיך להתגלגל הלאה, לא תיעצר אצל שני המשתמשים הראשונים.
- לשלוח אירוע Umami: `umami.track('compare_result_viewed', { compatibility: Math.round(compatibility * 100) })`.

### הערות/מגבלות ידועות (לא לתקן עכשיו, רק לדעת)
- זו אפליקציית SPA טהורה בלי server-side rendering, אז ה-OG tags הקבועים ב-`index.html` יוצגו גם לקישורי `?cmp=...` (אין preview דינמי ייחודי ללינק השוואה). זה בסדר ל-MVP — לא לבנות תשתית שרת בשביל זה.
- לוודא שהאורך של ה-URL המקודד לא מתפוצץ: `axisProfile` הוא בערך 15–20 צירים עם ערך אחד עשרוני, אז ה-JSON קטן (כמה מאות בייטים לפני base64) — לא צפויה בעיה, אבל שווה להדפיס באורך console.log באיזה שהוא טסט ידני כדי לוודא.

### בדיקה ידנית לפני שמסיימים
1. להריץ מקומית, לסיים שאלון, ללחוץ "השווה עם חבר", לאשר, להעתיק קישור.
2. לפתוח את הקישור בטאב incognito חדש — לוודא שמופיע הבאנר ב-Welcome ולא ההשוואה עצמה.
3. לענות על השאלון בטאב השני עד הסוף — לוודא שההשוואה מופיעה עם אחוז הגיוני ולא NaN/undefined.
4. לנסות לפתוח `?cmp=xxxinvalid` (זבל) — לוודא שהאתר עולה נורמלי בלי קריסה/שגיאה בקונסול.
