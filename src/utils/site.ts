// Single source of truth for the deployed URL, so a domain change is a one-line edit instead of
// a grep-and-replace across every component that builds a share link.
export const QUESTIONNAIRE_URL = 'https://matzpen-bchirot.vercel.app/';
